<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.dao.UserMapper">
    <sql id="insertFields">
        username,password,salt,email,type,status,activation_code,head_url,create_time
    </sql>
    <sql id="selectFields">
        id,username,password,salt,email,type,status,activation_code,head_url,create_time
    </sql>
    <select id="selectById" resultType="User">
        select <include refid="selectFields"></include>
        from USER
        where id = #{id}
    </select>
    <select id="selectByName" resultType="User">
        select <include refid="selectFields"></include>
        from USER
        where id = #{username}
    </select>
    <select id="selectByEamil" resultType="User">
        select <include refid="selectFields"></include>
        from USER
        where email = #{email}
    </select>
    <!--如果所用参数时bead需要用parameterType表示出来
    keyProperty的值写的是bean中对应表中主键名称，用于插入数据后把主键写回bean中
    -->
    <insert id="insertUser" parameterType="User" keyProperty="id">
        insert into user (<include refid="insertFields"></include>>)
        VALUES (#{username}, #{password}, #{salt}, #{email}, #{type}, #{status}, #{activationCode}, #{headerUrl}, #{createTime})
    </insert>
    <update id="updateStatus">
        update user set status = #
    </update>
</mapper>